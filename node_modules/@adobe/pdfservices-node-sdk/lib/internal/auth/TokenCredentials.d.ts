import { Credentials } from "../../auth/Credentials";
export declare class TokenCredentials implements Credentials {
    private readonly _clientId;
    private readonly _token;
    constructor(params: {
        clientId: string;
        token: string;
    });
    get clientId(): string;
    get token(): string;
}
