import { Region } from "../../Region";
export declare class PDFServicesUri {
    private static readonly URI;
    private static readonly US_URI;
    private static readonly EU_URI;
    private static readonly REGION_URI_MAP;
    static getURIForRegion(region?: Region): string;
    static get defaultUri(): string;
}
