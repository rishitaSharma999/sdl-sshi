import { PageRange } from "../../../params/PageRange";
export declare abstract class PageAction {
    private readonly _pageRanges;
    protected constructor(pageRanges: PageRange[]);
    get pageRanges(): PageRange[];
}
