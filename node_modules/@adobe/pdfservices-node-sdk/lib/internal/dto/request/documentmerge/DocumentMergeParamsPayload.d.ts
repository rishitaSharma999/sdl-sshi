export declare class DocumentMergeParamsPayload {
    protected readonly _jsonDataForMerge: JSON;
    protected readonly _outputFormat?: string;
    protected readonly _fragments?: JSON[];
    constructor(jsonDataForMerge: JSON, fragments?: JSON[], outputFormat?: string);
}
