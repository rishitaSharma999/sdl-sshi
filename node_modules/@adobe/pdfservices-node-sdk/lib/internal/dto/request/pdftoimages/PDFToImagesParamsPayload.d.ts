export declare class PDFToImagesParamsPayload {
    protected readonly _targetFormat: string;
    protected readonly _outputType?: string;
    constructor(targetFormat: string, outputType?: string);
}
