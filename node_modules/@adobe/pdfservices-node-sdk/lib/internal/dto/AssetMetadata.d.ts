export declare class AssetMetadata {
    private readonly _type;
    private readonly _size;
    constructor(params: {
        type: string;
        size: number;
    });
    get type(): string;
    get size(): number;
}
