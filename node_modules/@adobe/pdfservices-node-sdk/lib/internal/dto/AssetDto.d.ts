import { AssetMetadata } from "./AssetMetadata";
export declare class AssetDto {
    private readonly _assetMetadata;
    private readonly _downloadUri;
    private readonly _assetID;
    constructor(params: {
        assetMetadata: AssetMetadata;
        downloadUri: string;
        assetID: string;
    });
    get assetMetadata(): AssetMetadata;
    get downloadUri(): string;
    get assetID(): string;
}
