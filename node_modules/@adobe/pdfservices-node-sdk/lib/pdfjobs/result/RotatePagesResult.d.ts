import { PDFServicesJobResult } from "./PDFServicesJobResult";
import { Asset } from "../../io/Asset";
/**
 * This class encapsulates the result of {@link RotatePagesJob}.
 */
export declare class RotatePagesResult implements PDFServicesJobResult {
    private readonly _asset;
    /**
     * Constructs a new `RotatePagesResult` instance
     *
     * @param asset - result asset
     */
    constructor(asset: Asset);
    /**
     * Returns the result {@link Asset}
     *
     * @returns {@link Asset}
     */
    get asset(): Asset;
}
