import { PDFServicesJobParams } from "../PDFServicesJobParams";
import { PageRanges } from "../PageRanges";
/**
 * Parameters for deleting pages from a PDF using {@link DeletePagesJob}.
 */
export declare class DeletePagesParams implements PDFServicesJobParams {
    private readonly _pageRanges;
    /**
     * Constructs a new `DeletePagesParams` instance.
     *
     * @param params - The parameters for constructing an instance of `DeletePagesParams`.
     * @param params.pageRanges - The page ranges to be used for deleting pages. Cannot be undefined.
     */
    constructor(params: {
        pageRanges: PageRanges;
    });
    /**
     * Returns the page ranges to be used for deleting pages.
     *
     * @returns the page ranges to be used for deleting pages.
     */
    get pageRanges(): PageRanges;
}
